import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Customers",
};

const Customer = () => {
	return (
		<div>
			<h1 className="text-2xl font-bold">Customer Pages</h1>
		</div>
	);
};

export default Customer;
